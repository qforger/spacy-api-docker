export enum SpacyAPI {
    dependencies = '/dep',
    entities = '/ent',
    sents = '/sents',
    sents_dep = '/sents_dep',
    all_pos = '/all_pos',
    models = '/models',
    version = '/version'
}